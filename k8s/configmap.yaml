apiVersion: v1
kind: ConfigMap
metadata:
  name: complaint-config
  namespace: complaint-app
data:
  DJANGO_DEBUG: "False"
  CORS_ALLOW_ALL_ORIGINS: "False"
  CORS_ALLOWED_ORIGINS: "https://complaint.example.com"
  FRONTEND_URL: "https://complaint.example.com"
  NEXT_PUBLIC_API_URL: "https://complaint.example.com/api"
